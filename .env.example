# For Uniform CLI auth
UNIFORM_API_KEY=''

# Default team ID to use for Uniform CLI
UNIFORM_TEAM_ID=''

# Default project ID to use for Uniform CLI
UNIFORM_PROJECT_ID=''

# Optional: PokeAPI proxy URL for the multi-Pokemon edgehancer.
# When set, `pnpm edgehancer:build` bakes this into the bundle so the
# edgehancer calls the proxy batch endpoint instead of PokeAPI directly.
# Leave empty/unset to use the default direct PokeAPI fetch.
# POKEAPI_PROXY_URL='https://pokeapi-proxy.example.com'